version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - FLASK_ENV=development
      - MONGO_HOST=mongodb
    depends_on:
      - mongodb

  mongodb:
    image: mongo:latest
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
